# Statement audit: `resources/catlog/`

- **Date:** 2026-08-05
- **Entry audited:** `resources/catlog/README.md`
- **Canonical hash:** `cec4fec0715767c67ff644b524975cd3710a9025b050f00b3a079941e7216d46`
  (frontmatter `sha256`, re-verified against
  `resources/catlog/catlog.tar.gz` by `shasum -a 256`, match)
- **Scope:** source layer only. Every map bullet anchored inside
  `classical.tex:5-242`, `classical.tex:541-593`, and
  `dedsys.tex:16-559`, plus every entry under `## Content digests`.
  Outline-depth bullets outside those ranges carry no fine-grained
  claim and stay out of scope.
- **Items audited:** 106
- **Method:** each anchor opened in the vendored `.tex` file. Macro
  renderings decoded against `macros.tex` and `decls.tex`.
- **Prose gate:** `prose-lint.py` = 2.61 per 100 words (draft 4.46).
  95 of the 98 remaining long sentences are verbatim source quotes, as
  are every semicolon, every contraction, and the one em dash. Editing
  them would break the quotations, so they stay.

Verdict key. CONFIRMED means the claim matches the source at the cited
anchor. PARTIAL means the anchor and the topic hold, but one stated
detail does not match. NOT CONFIRMED means the claim fails.

## Section map, `classical.tex`

1. `classical.tex:5,541,576,582`: section-header anchors and labels.
   CONFIRMED. `l.5` `\section{Props and symmetric monoidal categories}`
   with `\label{sec:prop-smc}` at `l.6`. `l.541` `\section{Cyclic
   multicategories and cosubunary polycategories}`. `l.576`
   `\section{Classical logic}` with `\label{sec:classical}` at `l.577`.
   `l.582` `\section{Polycategories and linear logic}` with
   `\label{sec:cllin}` at `l.583`.

2. `classical.tex:5-242`: block heading, "the machinery `l.567-570`
   names as the route into a polycategory term syntax". CONFIRMED.
   `l.569-570`: "Get a term syntax for polycategories by mapping them
   into a prop and using a ``proof net'' condition to characterize the
   image. (Actually, use this as a lead-in to prop type theory.)"

3. `classical.tex:14-20`: Definition, prop (`defn:prop`). CONFIRMED.
   Source: "A prop consists of (1) A set of objects, and (2) A
   symmetric strict monoidal category (that is, a symmetric monoidal
   category whose associators and unitors are identities) whose
   underlying monoid of objects is freely generated by the set of
   objects of the prop." `item:prop2` sits at `l.18`. Sub-anchors also
   hold: `l.22-24` writes the objects of that category as finite lists
   with concatenation and empty-list unit, `l.26-27` records the
   one-object Adams-MacLane original and the dropped adjective
   "colored".

4. `classical.tex:31-33`: Definition, polygraph. CONFIRMED. Source
   `l.32`: "A polygraph 𝒢 is a set of objects together with a set of
   arrows, each assigned a domain and codomain that are both finite
   lists of objects." The gloss "the generating data a prop is freely
   built from" restates `l.29`, one line above the cited range: "The
   underlying data of a prop, from which we intend to freely generate
   one, is a polygraph."

5. `classical.tex:35-199`: motivating discussion. **PARTIAL.** The
   anchors inside the bullet all hold. `l.37` introduces "two
   plausible-looking derivations of a sequent representing f•g",
   `l.64` labels `eq:prop-bad-terms`, `l.66` says the equality "is not
   directional", and `l.136` gives the discipline: "The idea is to
   force ourselves to ``apply all functions as soon as possible''".
   Activeness at `l.141-169` and the built-in permutations at
   `l.183-198` also hold.
   Discrepancy, the bullet's lead-in clause. Entry text: "motivating
   discussion for why the free-prop derivation calculus needs a
   nontrivial equality relation on terms, not only on derivations".
   Source `l.35`: "In our type theories for categories and
   multicategories ... we did not have to impose any equivalence
   relation on the derivations. However, in the case of props, the
   interchange rule for • makes things more complicated." Source
   `l.67-69`: "We would like to avoid having to assume such equalities
   in ≡ ... a better approach is to design a different theory in which
   there is only *one* derivation of f•g." The source contrasts
   derivations in earlier chapters against props. It never asks for a
   term-level equality on top of a derivation-level one. It concludes
   that the design avoids the equality. The bullet's own closing clause
   states that conclusion correctly.

6. `classical.tex:201-236`: Figure, "Type theory for props"
   (`fig:props`). **PARTIAL.** Figure environment runs `l.201-236`,
   caption at `l.234`, label at `l.235`. The generator rule occupies
   `l.204-220` and the identity rule `l.222-232`. The permutation
   constraints hold: `l.213` "σ preserves the relative order of types
   in E⃗", `l.214` "σ preserves the relative order of F₁,…,G₁", and
   `l.215` `τ ∈ nShuf(h,…,k;Z⃗)`. The `l.238-241` and `l.242`
   sub-claims hold, including the quoted "This completes our
   definition of the type theory for props under 𝒢."
   Discrepancy, the activeness constraint. Entry text: "applies a batch
   of generators `f, …, g` (each needing at least one active input) and
   scalar generators `h, …, k`". The parenthetical attaches only to the
   non-scalar generators. Source `l.206` puts `\oneactv` on every input
   list, scalar ones included: "(A⃗^{★≥1},…, B⃗^{★≥1}, C⃗^{★≥1},…,
   D⃗^{★≥1}, E⃗)", where C⃗ and D⃗ are the domains of the scalar
   generators `h` and `k` at `l.209-210`. Source `l.145`: "we restrict
   the generator rule to require that at least one of the *inputs* of
   each generator being applied must be active in the premise." The
   matching digest entry (item 84 below) states this correctly.

## Section map, `classical.tex:541-593`

7. Block heading, section spans and the no-environment claim.
   CONFIRMED. Spans check out against the `\section` lines listed in
   item 1. `rg 'begin\{(defn|thm|lem|prop|cor|rmk|exer)'` over
   `sed -n '541,593p' classical.tex` returns nothing, so no theorem-like
   environment appears in the range.

8. `classical.tex:544`: two motivating examples. CONFIRMED. Source:
   "Motivate with two examples: multivariable adjunctions (symmetric),
   and classical logic (cartesian)."

9. `classical.tex:546-548`: cyclic-duality proposal. CONFIRMED.
   Source `l.547`: "a map ε : (A,A^•) → () is the counit of a duality
   if composing with it induces a bijection between arrows (Γ,A)→()
   and Γ → A^•, and the duality is symmetric if εσ is also such a
   counit." Source `l.548`: "Conullary arrows are mutual left
   adjunctions and proofs of contradictions; the universal property is
   precisely proof by contradiction."

10. `classical.tex:550-551`: open planning question and side remark.
    CONFIRMED. Source: "Maybe start with the cosubunary case at the
    beginning of the chapter, as the simplest one?" and "Note that if
    the conullary arrows are representable, and we have function-types,
    then all duals exist, but are not generally symmetric."

11. `classical.tex:553-557`: elim and intro for `A^•`. CONFIRMED.
    Source `l.553-554`: "The elim for A^• is ε, which ``applies'' a
    term of A^• to a term of A to produce a conullary term. Its intro
    ``abstracts'' a conullary term over a variable of A to a term of
    A^•, and the universal property is β/η for these." The
    parenthetical query sits at `l.555`. Source `l.556` covers the
    symmetric direction, `l.557` the Koh-Ong comparison: "it's more or
    less just making their substitutions implicit rather than
    explicit."

12. `classical.tex:559`: multivariable-adjunction instance. CONFIRMED.
    Source: "its two right adjoints are x:A, z:C^• ⊢ μy. z(f(x,y)) :B^•
    and y:B, z:C^• ⊢ μx. z(f(x,y)):A^•", with `l.560` "(We have to
    dualize their domains and codomains to regard them as *left*
    adjoints.)"

13. `classical.tex:562`: de Morgan duality line and the `\parr`
    provenance note. **PARTIAL.** The mathematical claim is exact.
    Source: "Adding connectives, we get de Morgan duality ∧/∨ for
    classical logic and ⊗/\parr in the noncartesian case, leading to
    linearly distributive and ∗-autonomous categories, and linear
    logic."
    Discrepancy, the provenance parenthetical. Entry text: "`\parr`
    (source macro, definition not located in the vendored `.tex` — no
    package `decls.tex` loads defines it)". The scope on `decls.tex` is
    the wrong place to look. `catlog.tex:2` reads
    `\usepackage{mathpartir,cancel,cmll,textcomp,environ}`. The `cmll`
    package is the standard linear-logic symbol package and is the
    macro's declared provider. `cmll.sty` itself is not vendored (only
    `mathpartir.sty` is), so this audit cannot read the definition off
    disk, and the "not located in the vendored source" half of the note
    stands. The note as written points a re-checker at `decls.tex`,
    which never loads `cmll`. The same macro also occurs at
    `simple.tex:2069` and `intro.tex:178`, outside the entry's mapped
    ranges.

14. `classical.tex:564-565`: stated defect of the cosubunary approach.
    CONFIRMED. Source: "abstracting over A^• produces a term in an
    *arbitrary* type, in contrast to how intro rules are supposed to
    live in their corresponding type former", tied at `l.565` to
    "the weirdness of classical logic by which ``proof by
    contradiction'' is a special rule that applies to any goal."

15. `classical.tex:567-573`: proposed remedy. CONFIRMED. Source
    `l.567-568`: "A cleaner approach from this POV, though yielding a
    messier term syntax, is to generalize from the cosubunary case to
    arbitrary polycategories. In a symmetric polycategory, a bijection
    between arrows (Γ,A) → Δ and Γ → (Δ,A^•) is automatically
    symmetric, since we can Yoneda to get a unit and counit with
    triangle identities." The proof-net proposal and the lead-in flag
    sit at `l.569-570`, "Mention the mix rules." at `l.571`, and
    "Mention negation normal form at least briefly, since it's in the
    literature." at `l.573`.

16. `classical.tex:576-580`: `sec:classical`, no compiled body.
    CONFIRMED. The only content is the commented line `l.579`:
    "%[Cartesian polycategories. I expect that the polycomposition
    coming from their definition as generalized polycategories includes
    the ``mix rule'', and is sufficient for a direct structural proof
    of cut admissibility.]"

17. `classical.tex:582-588`: `sec:cllin`, no compiled body. CONFIRMED.
    The content is `l.585-586` ("%[Mention linearly distributive
    categories and ∗-autonomous categories. But don't belabor them, and
    perhaps just cite references like~\cite{cs:wkdistrib} for their
    universal characterizations and initiality theorems.]") and `l.588`
    ("% Hughes, "Simple free star-autonomous categories and full
    coherence", if we want to do more.").

## Section map, `dedsys.tex`

18. `dedsys.tex:16,94,126,227,283,405`: section-header anchors.
    CONFIRMED. Each line carries the `\section` the entry names, in
    order: Trees and free algebras, Indexed trees, Free algebras with
    axioms, Rules and deductive systems, Terms, Variable binding and
    α-equivalence.

19. `dedsys.tex:4-13`: appendix framing. CONFIRMED. Source `l.4`:
    "giving precise meanings to words like ``judgment'', ``rule'',
    ``derivation'', and ``binder''." Source `l.7-8`: "Formally
    speaking, this appendix should come before \cref{chap:unary}.
    However, its technicalities seem unlikely to be appreciated without
    some concrete exposure to the ideas that it is trying to explain,
    so I have placed it as an appendix instead."

20. Subsection ranges and labels (`l.16-92` `sec:trees`, `l.94-125`
    `sec:indexed-trees`, `l.126-224` `sec:axioms`, `l.227-281`
    `sec:rules`, `l.283-403` `sec:terms`, `l.405-559` `sec:alpha`).
    CONFIRMED. Each range ends on the blank lines before the next
    `\section`. Each `\label` sits one line below its `\section`.

### Trees and free algebras

21. `dedsys.tex:23-25`: signature. CONFIRMED. Source: "A signature Σ
    is a set Σ₁ of operations with a function ar:Σ₁ → ℕ assigning to
    each operation a natural number", with the footnote across
    `l.23-25`: "Everything in this chapter works just as well if
    arities are arbitrary cardinal numbers (except that in
    \cref{sec:axioms} we would require the axiom of choice). However,
    for simplicity we restrict to the case of finite arities". The
    entry writes "work throughout" where the source scopes the claim to
    "this chapter".

22. `dedsys.tex:26-27`: Σ-algebra and its morphisms. CONFIRMED.
    Source: "A Σ-algebra is a set A together with, for each m∈Σ₁, a
    function [m]:A^{ar(m)} → A." and "There is an obvious notion of
    Σ-algebra morphism, forming a category." The pointed-magma example
    at `l.30` reads "if Σ₁={e,m} with ar(e)=0 and ar(m)=2, then
    Σ-algebras are *pointed magmas*".

23. `dedsys.tex:33-42`: tree vocabulary. CONFIRMED. Source `l.34`
    defines a tree by nodes plus a connected loop-free "edge existence"
    relation, `l.35` the root, `l.36` the unique non-retracing path and
    outgoing edge and parent, `l.37` incoming edges and children,
    `l.38` descendant, `l.41` "A branch is a non-retracing path
    starting at the root; a rooted tree is well-founded if there are no
    infinite branches."

24. `dedsys.tex:43-47`: Σ-labeled tree and `WΣ`. CONFIRMED. Source
    `l.43` gives the labeling function and the bijection from incoming
    edges to `{1,…,ar(m)}`. Source `l.45-46`: "We write WΣ for the set
    of all isomorphism classes of *well-founded* Σ-labeled trees. (Note
    that WΣ is empty unless there is at least one nullary operation.)"
    Source `l.47` grafts under a fresh root: "define a tree
    [m](t₁,…,t_{ar(m)}) with nodes {⋆} ⊔ ⨆ᵢ tᵢ, where ⋆ is the root,
    with label m". The source itself has a slip at `l.43`, "labeled by
    m∈𝒪" where 𝒪 should read Σ₁. The entry writes Σ₁ and so does not
    carry the slip.

25. `dedsys.tex:49-51`: initiality remark. CONFIRMED. Source: "The
    central fact is that WΣ is the initial Σ-algebra", the skippable
    classical proof at `l.50`, and `l.51`: "(From a constructive
    type-theoretic point of view, WΣ and its initiality are sometimes a
    fundamental axiom not reducible to sets.)"

26. `dedsys.tex:53-62`: `thm:tree-ind`. CONFIRMED. Source: "Suppose
    P⊆WΣ has the property that for any m and trees t₁,…,t_{ar(m)} such
    that each tᵢ∈P, then also [m](t₁,…,t_{ar(m)})∈P. Then P=WΣ." Proof
    `l.60-61`: "By the contrapositive of our assumption, therefore,
    there must be some i such that tᵢ∉P. Iterating, we obtain an
    infinite branch, contradicting well-foundedness."

27. `dedsys.tex:64-69`: `thm:tree-rec` and its stub proof. CONFIRMED.
    Source `l.65`: "For any Σ-algebra A, there is a unique Σ-algebra
    morphism WΣ → A." Proof body, `l.68`: "TODO: standard argument."

28. `dedsys.tex:71-74`: free algebra by `Σ[X]`. CONFIRMED. Source:
    "define a new signature Σ[X] by Σ[X]₁ = Σ₁ ⊔ X, where ar(x)=0 for
    all x∈X. Then a Σ[X]-algebra is just a Σ-algebra together with a
    map from X into its underlying set, so the initial such algebra is
    exactly the free Σ-algebra on X. Thus, the forgetful functor from
    Σ-algebras to sets has a left adjoint."

29. `dedsys.tex:76-80`: recursion reformulation. CONFIRMED. Source
    `l.76`: "given an arbitrary set A, to define a function WΣ → A it
    suffices to define a Σ-algebra structure on A". Source `l.79` gives
    the recursive form on `f([m](t₁,…,t_{ar(m)}))`.

30. `dedsys.tex:84-87`: `ex:wf-rigid`. CONFIRMED. Source: "Prove that
    a well-founded Σ-labeled tree has no nonidentity automorphisms.
    Thus, the passage to isomorphism classes in the definition of WΣ is
    ``categorically harmless''."

31. `dedsys.tex:89-91`: `ex:natw`. CONFIRMED. Source: "Exhibit a
    signature Σ such that WΣ ≅ ℕ and \cref{thm:tree-ind} reduces to
    ordinary mathematical induction."

### Indexed trees

32. `dedsys.tex:97-104`: multi-sort motivation. CONFIRMED. Source
    `l.98` gives the group acting on a set as two sorts. Source
    `l.101-103`: "for a fixed set 𝒪, we regard categories with object
    set 𝒪 as an algebraic structure whose set of sorts is 𝒪×𝒪. Thus
    each hom-set is a separate sort, and each triple A,B,C gives a
    different binary composition operation ∘_{A,B,C}".

33. `dedsys.tex:106-109`: multi-sorted signature. CONFIRMED. Source
    `l.106-107` adds Σ₀, the output sort `cₘ`, and the input sorts, and
    writes `m:(d_{m,1},…,d_{m,ar(m)}) → cₘ`. Source `l.108`: "From now
    on we call these *multi-sorted signatures* simply signatures; the
    simpler signatures of \cref{sec:trees} we re-christen one-sorted
    signatures." Source `l.109` gives the multigraph remark. The
    entry's note that `defn:multigraph` is not in this file holds:
    `rg` locates it at `simple.tex:147`.

34. `dedsys.tex:111`: multi-sorted Σ-algebra, with the `l.112-114`
    example. CONFIRMED. Source `l.111`: "a Σ-algebra is a Σ₀-indexed
    family of sets {Aᵢ} together with for each m∈Σ₁ a function
    A_{d_{m,1}} × ⋯ × A_{d_{m,ar(m)}} → A_{cₘ}." Source `l.114`: "then
    an indexed algebra consists of two sets A_g and A_s, a binary
    operation on A_g, and an action of A_g on A_s."

35. `dedsys.tex:116-118`: multi-sorted labeled tree and `WΣᵢ`.
    CONFIRMED. Source `l.116`: "if x is the k-th child of y, and x is
    labeled by m∈Σ₁ while y is labeled by p∈Σ₁, then c_m = d_{p,k}."
    Source `l.117-118`: "let WΣᵢ be the set of isomorphism classes of
    Σ-labeled trees for which the output sort of the root is i. Then
    {WΣᵢ} has a similar tautological Σ-algebra structure, and is the
    initial one."

36. `dedsys.tex:122-124`: `ex:multi-sorted-W`. CONFIRMED. Source:
    "Prove that {WΣᵢ} is the initial Σ-algebra."

### Free algebras with axioms

37. `dedsys.tex:129-131`: motivation. CONFIRMED. Source `l.130`: "The
    free monoid on a set X is naturally regarded as a quotient of the
    free pointed magma on X that forces associativity and unitality to
    hold." Source `l.131`: "we can construct free algebras of this sort
    quite generally by defining an equivalence relation *as another
    indexed free algebra*." The entry writes "congruence" where the
    source writes "equivalence relation", and the source establishes
    the congruence property at `l.155`.

38. `dedsys.tex:134-158`: worked free-semigroup case. CONFIRMED.
    Source `l.138` sets Σ^≡₀ = 𝔉_Mag X × 𝔉_Mag X. The five operation
    families sit at `l.140-144`, in the entry's order: nullary
    `() → (x,x)`, unary `((x,y)) → (y,x)`, binary
    `((x,y),(y,z)) → (x,z)`, binary `((x,y),(z,w)) → (x·z,y·w)`, and
    nullary `() → (x·(y·z),(x·y)·z)`. The algebra display sits at
    `l.146-153`. Source `l.155`: "The above elements and operations
    imply that this is an equivalence relation that is a congruence for
    the magma operation and moreover relates x·(y·z) to (x·y)·z for all
    x,y,z", with the converse at `l.156`. Source `l.158`: "Let ≡ denote
    the binary relation obtained as above from nonemptiness of the
    *initial* algebra for this indexed signature."

39. `dedsys.tex:160-177`: `thm:free-monoid`. CONFIRMED. Source `l.161`:
    "The quotient of 𝔉_Mag X by ≡ is the free semigroup generated by
    X." The entry keeps the source's own label name (`free-monoid`)
    beside the source's own statement (free semigroup). The proof runs
    as the entry describes: `l.165-168` inherits the operation and
    associativity, `l.170-176` factors ψ:X→M through φ and the algebra
    "R(x,y) means φ(x)=φ(y)", using initiality at `l.174`.

40. `dedsys.tex:179-182`: `(Σ,Λ)`-algebra. CONFIRMED. Source `l.180`:
    "we have additionally a set Λ of axioms, each of which is a pair
    (a,b) of elements of the free algebra WΣ[V]ᵢ for some i∈Σ₀ and some
    finite set V." Source `l.182`: "We define a (Σ,Λ)-algebra to be a
    Σ-algebra A such that ḡ(a)=ḡ(b) for any (a,b)∈Λ and g:V→A."

41. `dedsys.tex:184-191`: associativity axiom as a tree pair.
    CONFIRMED. Source `l.184-190` displays the two labeled trees inside
    `WΣ[{x,y,z}]`. Source `l.191`: "The (Σ,Λ)-algebras in this case are
    exactly semigroups."

42. `dedsys.tex:193-207`: general `Σ^≡` construction. CONFIRMED.
    Source `l.194`: "Σ^≡₀ = {(i,x,y) | i∈Σ₀; x,y∈WΣ[X]ᵢ}". Reflexivity,
    symmetry, and transitivity operations sit at `l.197-199`. The
    per-operation congruence sits at `l.200-204`. The nullary
    axiom-instance operation per axiom and per `g:V→WΣ[X]` sits at
    `l.205-206`. Source `l.208`: "Let ≡ᵢ be the binary relation on
    WΣ[X]ᵢ defined by a≡ᵢb if the sort (i,a,b) is nonempty in the
    initial Σ^≡-algebra."

43. `dedsys.tex:210-212`: `thm:tree-quotient`, with the `l.214`
    restatement. CONFIRMED. Source `l.211`: "Each ≡ᵢ is an equivalence
    relation and a congruence for the Σ-algebra structure, and the
    quotients WΣ[X]ᵢ/≡ᵢ form the free (Σ,Λ)-algebra.\qed". The `\qed`
    closes the theorem with no argument. Source `l.214` restates it as
    a recursive-definition principle with an axiom-instance check.

44. `dedsys.tex:218-220`: `ex:tree-quotient`. CONFIRMED. Source:
    "Prove \cref{thm:tree-quotient}."

45. `dedsys.tex:222-224`: `ex:infop-ac`. CONFIRMED. Source: "Why is
    the axiom of choice required to generalize \cref{thm:tree-quotient}
    to the case of infinitary operations?"

### Rules and deductive systems

46. `dedsys.tex:230-234`: the signature sequence. CONFIRMED. Source
    `l.232`: "We consider a sequence of signatures Σ⁽¹⁾,…,Σ⁽ⁿ⁾ for
    which the sorts of Σ⁽ᵏ⁾ are defined in terms of the initial
    algebras WΣ⁽ʲ⁾ for the previous signatures j<k." The example
    `Σ⁽²⁾₀ = WΣ⁽¹⁾ × WΣ⁽¹⁾` sits at `l.233`, the `(Σ⁽ʲ⁾)^≡` special
    case at `l.234`.

47. `dedsys.tex:236-246`: judgment and the notation table. CONFIRMED.
    Source `l.236`: "Each sort in one of the signatures Σ⁽ᵏ⁾ is called
    a judgment." All four table items check out. `l.239-241` gives the
    `A⊢B` sort for categories on 𝒪, the cut-ful reference, and the
    cut-free theory with the same sorts and different operations.
    `l.242-243` gives the `type` sort name and `Σ⁽²⁾₀ = WΣ⁽¹⁾ × WΣ⁽¹⁾`.
    `l.244` gives `Γ⊢Δ` where one or both sides is a list. `l.245`
    gives `x≡y:𝒥`.

48. `dedsys.tex:248-250`: rule, premises, conclusion. CONFIRMED.
    Source `l.248`: "each operation m:(𝒥₁,…,𝒥ₙ) → 𝒥′ in one of the
    signatures Σ⁽ᵏ⁾ is called a rule", drawn as an inference rule.
    Source `l.250` names the premises and the conclusion.

49. `dedsys.tex:252`: derivation. CONFIRMED. Source: "each element of
    WΣ⁽ᵏ⁾ is called a derivation (sometimes a derivation *of* its root
    judgment) and written by placing rules on top of each other to form
    its tree structure."

50. `dedsys.tex:253-269`: worked derivation-tree pairs, and the
    object-count observation. CONFIRMED. Source `l.253`: "if 𝒥 denotes
    the single sort of the signature for semigroups, then the
    associativity axiom of a monoid is". Source `l.262`: "associativity
    is the collection of axioms (one for each A,B,C∈𝒪)", while the
    displayed trees at `l.264-268` use four objects and the rules
    `∘_{A,B,D}`, `∘_{B,C,D}`, `∘_{A,C,D}`, `∘_{A,B,C}`. The entry's
    note of that mismatch is correct.

51. `dedsys.tex:271-274`: deductive system. CONFIRMED. Source `l.271`:
    "The whole sequence of signatures Σ⁽¹⁾,…,Σ⁽ⁿ⁾ is called a deductive
    system." Source `l.272` gives the semigroup-plus-monoid-axioms
    example. Source `l.273`: "Some deductive systems (probably not all)
    deserve to be called *type theories*; but we will not attempt to
    give any definition of this class except by the examples we
    consider (throughout the entire book)."

52. `dedsys.tex:275-280`: Remark on dependent type theories.
    CONFIRMED. Source `l.277`: "*dependent* type theories break the
    clean ``stratification'' ... since in the judgment ⊢A type the type
    A can now contain terms from the ``higher level'' judgment Γ⊢M:B."
    Source `l.278`: "the whole system must be defined by one big mutual
    induction (in type-theoretic lingo it is an ``inductive-inductive
    definition'')." Source `l.279`: "The general idea is similar,
    however."

### Terms

53. `dedsys.tex:286-288`: motivation for terms. CONFIRMED. Source
    `l.286-287`: "the notation would rapidly get unwieldy if each 𝒥 in
    a rule contained within it some number of derivation trees. Thus,
    we generally represent derivations by terms, which are a more
    concise syntax containing enough information to reconstruct the
    derivation." Source `l.288` gives `x·(y·z)` and `(x·y)·z` with the
    rule `m` written infix.

54. `dedsys.tex:292-293`: the `M:𝒥` convention and its exception.
    CONFIRMED. Source: "If M is a term representing a derivation of the
    judgment 𝒥, we generally write M:𝒥." and "(A notable exception is
    that if 𝒥 is the sort of a one-sorted Σ⁽¹⁾ presenting the objects
    of a category ... we usually write ``A type'' or ``⊢A type'' rather
    than ``A:type''.)"

55. `dedsys.tex:294-297`: the term-annotated semigroup rule.
    **PARTIAL.** The metavariable claim holds. Source `l.296`: "Here M
    and N are ``metavariables'' standing for terms".
    Discrepancy, the rendering of the rule. Entry text: "semigroup
    multiplication, `M : 𝒥, N : 𝒥 ⊢ M·N : 𝒥`". Source `l.295` is an
    inference rule, not a sequent: `\inferrule*[Right=$m$]{M:\cJ \\
    N:\cJ}{M\cdot N :\cJ}`, that is, the two premises `M:𝒥` and `N:𝒥`
    sit above the line and `M·N:𝒥` below it. The entry's own notation
    paragraph assigns `⊢` to the source macro `\types`, which is the
    judgment turnstile. This rendering spends that glyph on the rule
    bar instead. Every other rule in the appendix carries
    premise/conclusion wording in this entry (items 48 and 66). This
    line departs from the entry's own practice.

56. `dedsys.tex:299-303`: context terms as a variant notation.
    CONFIRMED. Source `l.299`: "``terms with variables from the
    context'' such as x:A⊢M:B can be regarded as merely a variant
    notation of something like x.M : (A⊢B)." Source `l.301`: "an
    equality judgment like x:A ⊢ M≡N:B is shorthand for (x.M)≡(x.N) :
    (A⊢B)." Source `l.302-303` defers α-equivalence to `sec:alpha`.

57. `dedsys.tex:307-323`: parsing against type-checking, with the
    cut-ful example. CONFIRMED. Source `l.307`: "There is no unique way
    to assign terms to a deductive system; all that is necessary is to
    describe some kind of syntax from which a derivation tree can be
    algorithmically extracted." Parsing is named at `l.311`,
    type-checking at `l.313-314`. Source `l.321` gives the one-sorted
    free algebra with `id_A` nullary and `∘_A` binary per `A∈𝒢₀`.
    Source `l.322`: "this free algebra contains many ``ill-typed''
    terms such as g∘_B id_A where g∈𝒢(C,D); the goal of type-checking
    is to discard these undesirables." Source `l.323` allows each
    judgment its own set of "potential terms".

58. `dedsys.tex:327-330`: dropping redundant subscripts. CONFIRMED.
    Source `l.328-330`: the technical form `h∘_{A,C,D}(g∘_{A,B,C}f)`,
    then "if we are given a term whose outer operation is a composition
    and that claims to represent a derivation of a judgment A⊢D, then
    the composition must be ∘_{A,?,D}. Thus in general it suffices to
    indicate the object being composed over, as in h∘_C (g∘_B f)."

59. `dedsys.tex:332-339`: Remark on further omissions. CONFIRMED.
    Source `l.333`: "if we know that h:C→D then a term of the form
    ``h∘(−)'' can only mean ``h∘_C(−)''." The commented-out footnote at
    `l.336-338` sketches bidirectional type-checking for a
    canonical/atomic calculus and does not compile.

60. `dedsys.tex:341-348`: the bottom-up reading. CONFIRMED. Source
    `l.346`: "they instead think ``if we want to deduce 𝒥, it suffices
    to have 𝒥₁ and 𝒥₂''." Source `l.347` gives the type-checking
    direction and the footnote flagging bidirectional theories, `l.348`
    the proof-search direction.

61. `dedsys.tex:354-360`: `defn:terms`, term system. CONFIRMED. Source
    `l.355`: "a term system for Σ is a Σ-algebra 𝕋, whose elements are
    called (potentially ill-typed) terms". Source `l.357`
    (`item:terms-uniq`): "For any judgment c∈Σ₀ and term t∈𝕋_c, there
    is at most one rule m:(d₁,…,dₙ)→c and terms sⱼ∈𝕋_{dⱼ} such that
    t=[m](s₁,…,sₙ)." Source `l.358` (`item:term-wf`) defines `s≺t` for
    `t=[m](s₁,…,sₙ)` and `s=sⱼ`, and requires it well-founded.

62. `dedsys.tex:362-364`: the morphism `WΣ → 𝕋`. CONFIRMED. Source
    `l.362-363`: "Since a term system 𝕋 is a Σ-algebra, there is a
    unique Σ-algebra morphism WΣ → 𝕋. This is the function that assigns
    to each derivation a unique representing term." The entry adds the
    attribution "(from `thm:tree-rec`)", which the source does not
    write at this line, and which matches the theorem's content.

63. `dedsys.tex:366-373`: `thm:term-system`. CONFIRMED. Source `l.367`:
    "If 𝕋 is a term system, then the unique Σ-algebra morphism WΣ → 𝕋
    is injective." Proof `l.371-372` uses `item:terms-uniq` to force
    the same head operation `m`, then structural induction on the
    arguments.

64. `dedsys.tex:375-376`: failure of the converse. CONFIRMED. Source:
    "the converse of \cref{thm:term-system} does not hold. Indeed, its
    conclusion does not even imply axiom \ref{item:terms-uniq} ...: the
    former is only about ``globally'' well-typed terms, while the
    latter is a ``local'' condition that says something even about
    ill-typed terms."

65. `dedsys.tex:378-389`: why the two axioms are stated as they are.
    CONFIRMED. Source `l.378`: "The reason for the extra strength
    of~\ref{item:terms-uniq}, and for condition~\ref{item:term-wf}, is
    to make type-checking a ``deterministic terminating recursive
    algorithm''", with the sketch at `l.379-382`. Source `l.388-389`
    covers computability and "Such a construction of 𝕋 generally also
    ensures axiom \ref{item:term-wf} immediately."

66. `dedsys.tex:391-398`: annotation of rules. CONFIRMED. Source
    `l.391`: "we regard the common ``annotation of rules'' as
    specifying a signature along with a term syntax for it." The
    annotated composition rule `ψ∘_B φ : (A⊢C)` sits at `l.395`, and
    `l.396` reads "if m is this rule, then the corresponding operation
    [m] on 𝕋 is given by the operation (−∘_B−)." Source `l.398`: "since
    in most cases 𝕋 is a free algebra for a different signature with an
    operation corresponding directly to each rule in Σ (though not in a
    one-to-one manner), we can generally omit this preliminary step".

### Variable binding and α-equivalence

67. `dedsys.tex:408-412`: framing and citations. CONFIRMED. Source
    `l.409`: "We could wave our hands at it by claiming to use de
    Bruijn variables everywhere, but this would be a bit dishonest."
    Source `l.410`: "we actually do use named variables all over the
    place". Source `l.412`: "our approach
    follows~\cite{gp:asib,gp:aswvb,pg:freshml} (see
    also~\cite{crole:alpha})." The no-titles claim holds:
    `tar tzf catlog.tar.gz` lists no `.bib` file, and `catlog.tex`
    calls `\bibliography{all}`.

68. `dedsys.tex:414-416`: the binding signature setup. CONFIRMED.
    Source `l.414`: "Let 𝔸 be a fixed infinite set (usually countable),
    whose elements we call variables." Source `l.415`: "Let Σ be a
    signature, one-sorted for simplicity, together with injective
    functions v,b:𝔸→Σ₁ such that ar(v(x))=0 and ar(b(x))=1 for all
    x∈𝔸." Source `l.416` states the intent for the initial Σ-algebra.

69. `dedsys.tex:418-421`: occurrence, binding, and the `match₊`
    example. CONFIRMED. Source `l.418`: "The inclusion v simply says
    that variables can occur in terms, while the operation b(x) is
    intended to ``bind'' the variable x in its argument; usually
    b(x)(M) is written x.M." Source `l.420-421`: "there will be
    operations match_{A+B} of arity 3, which we combine with two uses
    of b to represent the terms annotating +E".

70. `dedsys.tex:423-432`: the `Aut(𝔸)` action. CONFIRMED. Source
    `l.423-424`: "let Aut(𝔸) be the group of automorphisms
    (permutations) of the set 𝔸. We write the action of σ∈Aut(𝔸) on
    x∈𝔸 by x^σ." The two clauses sit at `l.427-428`, the catch-all at
    `l.431` ("In all other cases, σ·(−) simply recurses into all
    subtrees"), and the unproved claim at `l.432` ("It is easy to show
    that this is a group action").

71. `dedsys.tex:434-436`: freshness. CONFIRMED. Source `l.434`:
    "Because all operations in Σ have finite arity ... and all trees in
    WΣ are well-founded, only finitely many variables can occur in any
    such tree." Source `l.435-436`: "since 𝔸 is infinite, for any M∈WΣ
    there is some variable z∈𝔸 that does not occur in M. We call such a
    z fresh (relative to M) and write z∉M."

72. `dedsys.tex:438-448`: α-equivalence and `eq:alpha-gen`. CONFIRMED.
    Source `l.438-440`: "We now define α-equivalence ≡ on WΣ, by
    defining a new signature Σ^≡ similar to how we did it in
    \cref{sec:axioms}. We include operations making ≡ a congruence for
    all operations of Σ except b. In the case of v, this means we have
    ``reflexivity at variables'' v(x)≡v(x)." The rule at `l.443-444`
    has premises `z∉M`, `z∉N`, `z≠x`, `z≠y`, `(zx)·M ≡ (zy)·N` and
    conclusion `b(x)(M) ≡ b(y)(N)`. Source `l.446` names `(zx)` and
    `(zy)` as transpositions.

73. `dedsys.tex:450-452`: worked α-equivalence examples. CONFIRMED.
    Source `l.450`: "x.x and y.y are α-equivalent because (zx)·x = z
    and (zy)·y = z." Source `l.451`: "the inner bound x really does
    ``shadow'' the outer one, making the latter invisible even though
    it has the same name." Source `l.452`: "neither of these is
    equivalent to x.(y.x), since (zx)·(y.x) = (y.z)."

74. `dedsys.tex:454-456`: free renaming remark. CONFIRMED. Source
    `l.454`: "if M∈WΣ, then x.M is α-equivalent to y.((yx)·M) for any
    variable y not occurring in M", with the one-line transposition
    calculation at `l.455`.

75. `dedsys.tex:458-459`: no primitive equivalence operations.
    CONFIRMED. Source: "Unlike in \cref{sec:axioms}, we do not
    explicitly include operations making ≡ an equivalence relation.
    However, we can nevertheless prove that it is; this is itself a
    sort of cut-admissibility."

76. `dedsys.tex:461-472`: `thm:alpha-adm`, seven properties.
    CONFIRMED. The seven items appear in the entry's order and carry
    the entry's labels: equivariance (`item:alpha-eqvadm`), congruence
    for binding (`item:alpha-bindadm`), invertibility of
    `eq:alpha-gen` (`item:alpha-gen-inv`, "if x.M≡y.N, then (zx)·M ≡
    (zy)·N for some fresh z"), reflexivity, symmetry, transitivity, and
    renaming (`item:alpha-rename`, "if z∉M then x.M ≡ z.((zx)·M)").

77. `dedsys.tex:473-505`: the proof of `thm:alpha-adm`. CONFIRMED.
    Source `l.474`: "Perhaps surprisingly, the tricky and important one
    is~\ref{item:alpha-eqvadm}." The fresh choice `w := z^σ` sits at
    `l.478` and `τ = (z^σ y^σ)σ(zx)` at `l.479`. Source `l.488` makes
    congruence-for-binding immediate. Source `l.489`: "\ref{item:alpha-
    gen-inv} is clear since~\eqref{eq:alpha-gen} is the only rule that
    can produce an α-equivalence between terms of the form x.M and
    y.N". Source `l.490` derives reflexivity and symmetry. Source
    `l.500-501` states the height induction for transitivity, since the
    equivariance-produced derivations "are not subtrees of our given
    derivations". Source `l.503-504` gives the renaming calculation
    through a fresh `w` and reflexivity.

78. `dedsys.tex:507-509`: the quotient. CONFIRMED. Source `l.507-508`:
    "The quotient WΣ/≡ of this equivalence relation is, of course, our
    set of ``terms modulo α-equivalence of bound variables''. Since ≡
    is a congruence for all the operations of Σ, these all descend to
    the quotient, including (by
    \cref{thm:alpha-adm}\ref{item:alpha-bindadm}) variable binding".

79. `dedsys.tex:510-518`: binder-restricted term sets. CONFIRMED.
    Source `l.512`: "We also usually want to restrict to some subsets
    of terms that have the right number of variables bound to represent
    the context." Source `l.517`: "we should take our set 𝕋 of terms to
    be the subset of WΣ/≡ consisting of terms having a variable binding
    outermost." Source `l.518`: "in a simple type theory the terms for
    Γ⊢B should have n variable bindings outermost, where n is the
    length of Γ (this is why in \cref{sec:terms} we allowed different
    judgments to have different sets of potential terms)." The source
    has a slip at `l.514`, "x:M:(A⊢B)" for "x.M : (A⊢B)". The entry
    writes the corrected form, which matches `l.299`.

80. `dedsys.tex:520-546`: building term operations, `match₊` and `×I`.
    CONFIRMED. Source `l.524`: "Since the rule +E has three premises,
    what we have to give is an operation 𝕋×𝕋×𝕋 → 𝕋, where 𝕋 is the set
    of α-equivalence classes of terms of the form x.M." Source
    `l.529-530` chooses representatives with `x` absent from `u.P` and
    `v.Q`, citing `item:alpha-rename`. Source `l.533-534` checks
    representative-independence through `item:alpha-gen-inv` "using the
    congruence rules and transitivity", with the calculation at
    `l.535-540`. Source `l.544-545`: "the term operation representing
    ×I takes as given x.M and y.N, but its output has only one shared
    variable ... and then write z.⟨(zx)·M,(zy)·N⟩ for the pairing
    term."

81. `dedsys.tex:548-552`: closing remark. CONFIRMED. Source `l.549`:
    "Since 𝕋 is a subset of an initial algebra, and our operations are
    built using at least one operation of that algebra,
    \ref{defn:terms}\ref{item:term-wf} is straightforward." Source
    `l.550-551`: "the proof of \ref{defn:terms}\ref{item:terms-uniq}
    essentially means checking that we chose the operations of the term
    signature to contain enough information to reconstruct a derivation
    step-by-step. This is a formal version of what in the main text we
    called *type-checking is possible* or *terms are derivations*."
    Source `l.552`: "since all our ``terms'' are actually
    α-equivalence classes, we never have to prove anything about
    α-equivalence."

## Content digests, `classical.tex`

82. `classical.tex:14-20`: prop. CONFIRMED. The two-part definition
    matches `l.15-19` word for word in content. Two notes. First, the
    clause "the morphisms of the prop are the morphisms of this
    monoidal category" does not appear at `l.14-20`. It follows from
    the definition, and the source uses it later at `l.502` ("If A is
    an object of a prop, a dual of A is an object A^* with morphisms
    η:()→(A,A^*) and ε:(A^*,A)→()"). Second, "Objects are written as
    finite lists" compresses `l.22`, "We write the objects of the
    monoidal category in~\ref{item:prop2} as finite lists (A,B,…,Z) of
    generating objects." The map bullet (item 3) keeps the distinction.

83. `classical.tex:31-33`: polygraph. CONFIRMED. Same source text as
    item 4.

84. `classical.tex:201-236`: the type theory for props under 𝒢.
    CONFIRMED. The judgment shape `Γ ⊢^𝔅 (M⃗ | Z⃗) : Δ` matches the
    figure. The label set is `l.96`: "we annotate our judgments with a
    finite subset 𝔅⊆𝔸 indicating which labels might have been used
    already in the terms". Scalars are `l.77-78`: "we write for
    instance (f(x),g(y) | h(z)) to mean that h(z):() ... We refer to
    terms of this sort as scalars." The per-generator activeness
    requirement matches `l.145`, and the two σ order conditions match
    `l.213-214`. The naming claim matches `l.242`.

85. `classical.tex:246-248`: `thm:prop-tad`. CONFIRMED. Source `l.247`:
    "If there is some assignment of activeness to the types in Δ such
    that Γ⊢^𝔅 (M⃗|Z⃗):Δ is derivable in the type theory for props, then
    that assignment is unique, as is the derivation."

86. `classical.tex:316-318`: `thm:prop-cutadm`. CONFIRMED. Source
    `l.317`: "Cut is admissible: given derivations of Γ⊢Δ and Δ⊢Φ, we
    can construct a derivation of Γ⊢Φ."

87. `classical.tex:417-419`: `thm:prop-cutissub`. CONFIRMED. Source
    `l.418`: "If Γ ⊢ (M⃗|Z⃗):A⃗ and x⃗:A⃗ ⊢ (N⃗|W⃗):B⃗, then the
    composite constructed by \cref{thm:prop-cutadm} is Γ ⊢
    (N⃗[M⃗/x⃗]|W⃗[M⃗/x⃗],Z⃗):B⃗." Pre-terms are defined at `l.401-408`
    and substitution at `l.409-414`, so the digest's "on pre-terms" is
    correct.

88. `classical.tex:458-474`: `thm:prop-moncat` and `thm:prop-initial`.
    CONFIRMED. Source `l.459`: "The contexts and derivable term
    judgments in the type theory for props under 𝒢, modulo the above
    equality rule, form a symmetric strict monoidal category." The
    "above equality rule" is the scalar-permutation rule at
    `l.446-448`. Source `l.470`: "Thus we have a prop, which we denote
    𝔉_Prop 𝒢." Source `l.473`: "𝔉_Prop 𝒢 is the free prop generated by
    𝒢."

89. `classical.tex:546-548`: cyclic-duality proposal. CONFIRMED. Same
    source text as item 9.

90. `classical.tex:553-557`: elim and intro for `A^•`. CONFIRMED. Same
    source text as item 11.

91. `classical.tex:559`: multivariable-adjunction instance. CONFIRMED.
    Same source text as item 12.

92. `classical.tex:567-570`: polycategory-via-prop proposal.
    CONFIRMED. Same source text as item 15, restricted to `l.567-570`.

93. `classical.tex:576-588`: `sec:classical` and `sec:cllin`.
    CONFIRMED. Same source text as items 16 and 17.

## Content digests, `dedsys.tex`

94. `dedsys.tex:23-26`: signature and Σ-algebra. CONFIRMED. Same
    source text as items 21 and 22.

95. `dedsys.tex:33-49`: tree, Σ-labeled tree, `WΣ`. CONFIRMED. The
    grafting structure is `l.47` and initiality is `l.49`, "The central
    fact is that WΣ is the initial Σ-algebra." The digest's "free/
    initial" pairs that with `l.33`, "Free Σ-algebras are conveniently
    described in terms of trees."

96. `dedsys.tex:53-56`: `thm:tree-ind`. CONFIRMED. The theorem
    environment runs `l.53-56` and the statement matches item 26.

97. `dedsys.tex:64-68`: `thm:tree-rec`. CONFIRMED. Statement at
    `l.65`, stub proof "TODO: standard argument." at `l.68`.

98. `dedsys.tex:71-74`: free algebra via `Σ[X]`. CONFIRMED. Same
    source text as item 28.

99. `dedsys.tex:106-118`: multi-sorted signature and indexed `WΣᵢ`.
    CONFIRMED. The operation typing is `l.107`. The initiality claim is
    `l.118`, and `l.122-124` sets it as `ex:multi-sorted-W`. The digest
    marks it as claimed.

100. `dedsys.tex:179-212`: `(Σ,Λ)`-algebra and `thm:tree-quotient`.
     CONFIRMED. Axioms as pairs in `WΣ[V]ᵢ` are `l.180`. The auxiliary
     nonemptiness relation is `l.208`. The quotient statement is
     `l.211`, and the `\qed` on that line closes the theorem with no
     argument.

101. `dedsys.tex:236-274`: judgment, rule, derivation, deductive
     system. CONFIRMED. Judgment `l.236`, rule `l.248`, derivation
     `l.252`, deductive system `l.271`. The digest writes "each built
     from the initial algebras of the earlier ones", which compresses
     `l.232`, "the sorts of Σ⁽ᵏ⁾ are defined in terms of the initial
     algebras WΣ⁽ʲ⁾ for the previous signatures j<k". The map bullet
     (item 46) carries the precise form.

102. `dedsys.tex:354-360`: `defn:terms`. CONFIRMED. Same source text
     as item 61.

103. `dedsys.tex:366-368`: `thm:term-system`. CONFIRMED. The lemma
     environment runs `l.366-368` and the statement matches item 63.

104. `dedsys.tex:414-431`: binding signature and permutation action.
     CONFIRMED. The injective `v,b:𝔸→Σ₁` with arities 0 and 1 is
     `l.415`. The action clauses are `l.427-428`. The catch-all
     recursion is `l.431`.

105. `dedsys.tex:438-445`: α-equivalence. CONFIRMED. The congruence
     scope is `l.439` and the swap rule is `l.442-445`, with premises
     and conclusion as the digest states them.

106. `dedsys.tex:461-472`: `thm:alpha-adm`. CONFIRMED. The seven
     properties match item 76.

## Tally

**102/106 CONFIRMED.** 4 PARTIAL (items 5, 6, 13, 55). 0 NOT
CONFIRMED.

The four PARTIAL items in one line each:

- Item 5, `classical.tex:35-199`: the bullet's lead-in says the
  calculus "needs a nontrivial equality relation on terms, not only on
  derivations". The source avoids that equality by design.
- Item 6, `classical.tex:201-236`: the "at least one active input"
  constraint is attached to the non-scalar generators only. The source
  applies it to every generator.
- Item 13, `classical.tex:562`: the `\parr` provenance note scopes the
  search to `decls.tex`. The load site is `catlog.tex:2`, which loads
  `cmll` (not vendored).
- Item 55, `dedsys.tex:294-297`: the entry renders an inference rule as
  a sequent with `⊢`. That glyph belongs to the source macro `\types`.

Macro decoding used for this audit, all read off disk. `macros.tex:13-17`
gives `\ay`=ar, `\sig`=Σ, `\axes`=Λ, `\act m`=[m], `\equivsym`=≡.
`macros.tex:25` gives `\types`=⊢, `macros.tex:48` gives `\ptens`=•, and
`macros.tex:134` gives `\d`=raised •. `macros.tex:49-53` gives the
activeness family, and `macros.tex:83,92,93` gives `\pair`, `\acase`,
and `\case`. The letter-family generators sit at `decls.tex:107-134`.
