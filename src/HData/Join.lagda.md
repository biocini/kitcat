The join of two types as a higher inductive type.

```agda
{-# OPTIONS --safe --erased-cubical --no-guardedness #-}

module HData.Join where

open import HData.Join.Type public

module Join where
  open import HData.Join.Base public
  open import HData.Join.Properties public
```
