# Changelog — Bb.VgCategoryShape

Every alteration and addition to this tree after it entered the
archive. **Newest entry first.** The tree is frozen green, so an
entry names the checker run that says so.

---

## 2026-07-28 — the archive process established, and the TODO became the README

**The process, applied.** `README.md` written to the common format
of `src/Bb/CLAUDE.md`. `TODO.md` converted into it and deleted.
The still-true content carried over whole. That is the record and
its structure-property split, the derivation order, and what
`Parity` shows. It also covers the two unwritten
constructions, `Mag.Op` with its route and the promotion out of
`Mag.*`. The re-founding program, the comparison with
`Cat.Logic`, and the three open questions carried too. This file
opened. `Bb.index` gained the tree's 4 imports and checks clean.

**Dangling citations, for the maintainer.**
`src/Cat/Logic/TODO.md` cites `src/Bb/VgCategoryShape/TODO.md` at
four sites, one of them naming it the program of record for the
`Mag` rebuild. Those citations now need to point at
`src/Bb/VgCategoryShape/README.md`. The file is outside this
tree, so the repointing was not made here.

**State, `verified`.** `just check-tree src/Bb`: 86 of 86 modules
typecheck, the 4 here among them. No module changed.

Next: the `Mag` rebuild starts fresh at `src/Mag`, gated on the
balanced layer of `Cat.Logic`, investigation line 5.

## 2026-07-28 — the Mag staging tree vendored

**The vendoring, `verified`** (commit `7fae4ef`). Four modules
from an untracked `Mag` staging directory, frozen green:
`Type`, `Base`, `Unit`, and `Parity`. Module paths read `Mag.*` in
the staging record and resolve here as `Bb.VgCategoryShape.*`. The
tree arrived with its `TODO.md`, under an archive banner.
