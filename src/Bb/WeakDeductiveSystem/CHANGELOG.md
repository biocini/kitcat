# Changelog — Bb.WeakDeductiveSystem

Every alteration and addition to this tree after it entered the
archive. **Newest entry first.** The tree is frozen green, so an
entry names the checker run that says so.

---

## 2026-07-28 — the archive process established, and the TODO became the README

**The process, applied.** `README.md` written to the common format
of `src/Bb/CLAUDE.md`. `TODO.md` converted into it and deleted.
The still-true content is the account of the carrier, the three
tiers, and the twelve `Gist` spikes. It also covers the record
cut that put the tree here. One block did not carry: the
successor's open-item list, live at `src/Cat/Logic/TODO.md` and
never this tree's. This file opened. `Bb.index` gained the tree's
16 imports and checks clean.

**State, `verified`.** `just check-tree src/Bb`: 86 of 86 modules
typecheck, the 16 here among them. No module changed.

Next: nothing planned. The tree stands as the stratum below the
(D′) cut, and `Cat.Logic` carries the live line.

## 2026-07-28 — the tree frozen at the (D′) record cut

**The freeze, `verified`** (commit `1db09db`, `just check-tree
src/Bb`: 16 of 16). `Cat.Logic` as it stood before the record cut
of the same day, copied whole: 16 modules, and the first tree in
the new `Bb.*` namespace. Lane ruled that the live tree carries no
red modules. Five free-framing `Gist` spikes moved here rather
than break under the cut: `AssociatesCountermodel`, `FramedCut`,
`FramedGroup`, `NeutralUnit`, and `TwistFidelity`. The `docs/`
files that cite the five point here. Session log:
[`notes/2026-07-28-balanced-record-cut.md`](../../../notes/2026-07-28-balanced-record-cut.md).

**Risk, recorded and accepted.** `Graph`, `Display`,
`Gist.FramedGroup`, and `Gist.RxDict` import the live
`Cat.Graph.Refl`. A change there can break this tree.
