# Changelog — Bb.OneTwist

Every alteration and addition to this tree after it entered the
archive. **Newest entry first.** The tree is frozen green, so an
entry names the checker run that says so.

---

## 2026-07-28 — the archive process established

**The process, applied.** `README.md` written to the common format
of `src/Bb/CLAUDE.md`. It carries the carrier, the countermodel
that decided against it, and the two spikes' dependence on
`Bb.WeakDeductiveSystem.Gist`. The tree had no `TODO.md`, so
nothing needed conversion. This file opened. `Bb.index` gained the
tree's 3 imports and checks clean.

**State, `verified`.** `just check-tree src/Bb`: 86 of 86 modules
typecheck, the 3 here among them. No module changed.

Next: nothing planned. The proposal lost.

## 2026-07-28 — the rejected carrier archived out of Test

**The move, `verified`** (commit `006d477`). Three `Test` spikes
became the tree: `ExtractedTwist` is `Base`,
`ExtractedTwistCancel` is `Cancel`, and `ExtractedTwistModels` is
`Models`. Git tracked the first alone, so the commit records one
rename and two additions. The same commit re-points the citations
in `docs/deductive-systems/` (`framing.md`, `invertibility.md`,
`mediation.md`, and `virtual-graphs.md`).

The spikes ran on 2026-07-27 and settled the one-twist proposal
against the five-field carrier. Session log:
[`notes/2026-07-27-one-twist-verdict.md`](../../../notes/2026-07-27-one-twist-verdict.md).
