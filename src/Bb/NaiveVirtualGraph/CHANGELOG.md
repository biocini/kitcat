# Changelog — Bb.NaiveVirtualGraph

Every alteration and addition to this tree after it entered the
archive. **Newest entry first.** The tree is frozen green, so an
entry names the checker run that says so.

---

## 2026-07-28 — twelve spikes vendored from Test, the carrier extracted, the archive process started

**The vendoring, `verified`.** Twelve `Test` spikes moved here as
`Gist` modules with the `Spike` prefix dropped:
`AbsorbObstruction`, `CrossedUnit`, `DeductiveSystem`,
`JudgmentLens`, `PathGroupoid`, `PerHandUnit`, `ReflexiveVG`,
`SelfUnit`, `StabilityShape`, `StableFiber`, `TwoSided`, and
`UnitCanonical`. Each had carried its own copy of the chosen-edge
carrier. The copies agreed, and the shared one is now
`Bb.NaiveVirtualGraph.Base`, a new module. Ten of the twelve read
the carrier from there. `PerHandUnit` and `SelfUnit` keep their
own, which differ from it. Thirteen modules in the tree. `just
check-tree src/Bb`: 86 of 86 modules typecheck, these among them.

**The archive process, applied.** `README.md` written to the
common format of `src/Bb/CLAUDE.md`, carrying the origin of the
spikes and the negative-result dossier they hold. The tree had no
`TODO.md`, so nothing needed conversion. This file opened.
`Bb.index` gained the tree's 13 imports and checks clean.

**Uncommitted at this writing.** The move and the new `Base` sit
in the working tree.

Next: nothing planned. The successor stratum is
`Bb.WeakDeductiveSystem`, and the live line is `Cat.Logic`.
