Scratch: build the full-triangle face₂₃ from gauge-r via the probe
bridge. EU (unitl fiber-square → path) + UL + bridge + INNER → coh →
w/v. face₂₃ itself assembled in Triangle where the fibers live.

Scratch file — not in All.

```agda
{-# OPTIONS --safe --erased-cubical --no-guardedness #-}

module Test.Face23Probe-20260711 where

open import Core.Type
open import Core.Base
open import Core.Data.Sigma
open import Core.Kan using (is-contr→is-prop; _∙_; module Path; pcom; hcom)
open import Core.Path.Base using (ap-comp)

open import Cat.Type
open import Cat.Codep.Coherent

module _ {o h} (C : hcategory o h) (A2 : hcategory-2-coherent C) where
  open hcategory C
  open hcategory-2-coherent A2

  module _ {x y z} (f : hom x y) (g : hom y z) where
    e : hom y y
    e = idn y

    P₀ : emb (e ⨾ g) ≡ emb g
    P₀ = emb-comp e g ∙ emb-idn-absorb g

    Ug : PathP (λ _ → fiber emb (emb g)) ((e ⨾ g) , P₀) (g , refl)
    Ug = is-contr→is-prop (emb-image-contr g) _ _

    -- ap fst Ug is definitionally unitl g
    unitl-is-apfst : ap fst Ug ≡ unitl g
    unitl-is-apfst = refl

    -- EU: the unitl fiber-wit23 square, read as a path.
    EU : ap emb (unitl g) ≡ P₀
    EU t s = hcom (∂ t ∨ ∂ s) λ where
      k (t = i0) → Ug s .snd i0
      k (t = i1) → P₀ s
      k (s = i0) → emb (e ⨾ g)
      k (s = i1) → Ug (~ t) .snd (t ∨ k)
      k (k = i0) → Ug (s ∧ ~ t) .snd (s ∧ t)

    -- The pre-side bridge, per context slot b.
    module bridged {v' : ob} (b : hom z v') where
      δ₀ : ctx y z
      δ₀ = (ctr y , (v' , b))

      c₀ : hom y v'
      c₀ = pre g b

      -- happly of emb-idn-absorb at the center: the emb-idn-absorb
      -- reading baked into the library unitl g.
      K-absorbr : pre (idn y) c₀ ≡ c₀
      K-absorbr = happly (emb-idn-absorb g) δ₀

      -- bridge: K (absorb-r form) ≡ absorb-l c₀ (post-eval form), via
      -- gauge-r. The K-is-absorbr / absorb-l-posteval refls collapse.
      bridge : K-absorbr ≡ absorb-l c₀
      bridge =
        ap (interchange (idn y) g (idn y) b ∙_)
          (ap (ap (λ a' → emb g ((y , a') , (v' , b)))) gauge-r)
        ∙ sym (absorb-lcoh g b)

      -- UL: ap of pre along unitl, read through EU at the center.
      UL : ap (λ hh → pre hh b) (unitl g) ≡ pre-comp e g b ∙ K-absorbr
      UL = ap (λ p → happly p δ₀) EU

      INNER : pre-comp e g b ∙ absorb-l c₀
            ≡ ap (λ hh → pre hh b) (unitl g)
      INNER = ap (pre-comp e g b ∙_) (sym bridge) ∙ sym UL

    LHS' : emb f · (e ⨾ g) ≡ emb f · g
    LHS' = ·-comp (emb f) e g ∙ ap (_· g) (·-idn (emb f))

    RHS' : emb f · (e ⨾ g) ≡ emb f · g
    RHS' = ap (emb f ·_) (unitl g)

    CP : (γ : ctx x z) → happly LHS' γ ≡ happly RHS' γ
    CP γ =
      sym (ap-comp F₀ (pre-comp e g bγ) (absorb-l (pre g bγ)))
      ∙ ap (ap F₀) (bridged.INNER bγ)
      where
        bγ : hom z (γ .snd .fst)
        bγ = γ .snd .snd
        F₀ : hom y (γ .snd .fst) → res γ
        F₀ k = emb f (γ .fst , (γ .snd .fst , k))

    coh : LHS' ≡ RHS'
    coh i j γ = CP γ i j

    -- w and v: the two contr-face endpoint witnesses.
    wit23 : (i : I) → emb (f ⨾ (unitl g i)) ≡ emb f · g
    wit23 i = emb-comp f (unitl g i) ∙ ap (emb f ·_) (λ j → unitl g (i ∨ j))

    w : (emb-comp f (e ⨾ g) ∙ ·-comp (emb f) e g ∙ ap (_· g) (·-idn (emb f)))
      ≡ wit23 i0
    w = ap (emb-comp f (e ⨾ g) ∙_) coh

    v : wit23 i1 ≡ emb-comp f g
    v = Path.unitr (emb-comp f g)
```
