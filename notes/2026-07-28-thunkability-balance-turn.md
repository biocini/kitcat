# 2026-07-28: thunkability to the balance turn

Objective at open: investigate line 2 of `src/Cat/Logic/TODO.md`,
thunkability as property or data. The session cascaded from that
verdict through the `Gist` namespace, the initial-model and
internal-language programs, the balance dossier, and the evidence
that ratified the (D′) record cut executed by a sibling session.

## What was done

1. **Line 2 settled: thunkability is data.** The length-4
   compatibility square `compat` was stated over any carrier with
   both cuts, and the circle model (one object, homs the circle,
   reflection through `mult`) refuted propositionality of
   `thunkable` and of the square-refined closure in a full weak
   deductive system. `Cat.Logic.Gist.ThunkableSquare`, with the
   verdict and residue in the TODO.
2. **`Cat.Logic.Gist` created.** Nine certified spikes vendored
   from `Test` with prefixes dropped, `docs/` citations swept.
   After the record cut, seven live there and five retired to the
   archive.
3. **Programs written into the TODO** so later sessions start
   cold: the initial-model program (line 9, coherence conjecture,
   word model, morphisms, free HIT), the internal-language seam
   (CatColab RFC 0004, five inquiry lines), the balance dossier
   under line 5 (independence facts, compression program, torsor
   argument, positions (D) and (D′)), and the `Mag` re-founding
   program, now superseded by the archive reorganization.
4. **Rulings recorded.** Mode separation is not a candidate
   foundation. Strict op-involution is not required, up to a path
   is acceptable, and the cut then kept it strict anyway. Balance
   enters as structure in `virtual-graph`, never in the predicate.
5. **Delegated spike sessions, prompts engineered here.** Three
   staged, checker-first prompts produced `Gist.ReadbackTorsor`,
   `Gist.BalancedBase`, and `Gist.BalancedProfile` in sibling
   sessions. Their results ratified (D′) and are recorded in the
   TODO dossier and `notes/2026-07-28-balanced-record-cut.md`.
6. **Mechanical close-out by subagent.** The handedness swap
   adjudicated stale, executed long ago in the rename pass, the
   scope section now a settled block with evidence lines. Ledger
   entries T32, T33, T34 added and T31's placement corrected. The
   `mmmm-classical-notions` rename landed with custody clean. The
   future/buffer register compression was fixed at its single live
   site.
7. **Resources custody.** `resources/kiselyov-having-effect/`
   vendored, PROVISIONAL, and the format authority's schema now
   admits `html`. The effects-as-interactions anchor entered the
   seam: variables as `ReqVar` requests, `var` as the pending
   read, `is-stable` as a stable-denotation condition.

## Strongest findings and decisions

- The freedom in a thunkability witness is a loop-space action,
  and uniform shifts are natural, so no coherence tower truncates
  it. A propositional thunkability must demand strictly more than
  inhabitation. This decided line 2 and forecast the balance
  torsor.
- The balance moduli is content. Coherent readbacks over the
  circle are a torsor over the centre, so the contractible form of
  balance defines uniquely-balanced systems and excludes the phase
  fragment. Balance therefore enters as structure, which the (D′)
  cut then adopted.
- At (D′) strength the absorption layer evaporates into theorems:
  the tier centres read back as the other twists. The sentence
  that opened the balance inquiry, that nothing identifies the
  centre with the other twist, is resolved by proof at (D′).
- Failed strategy, with reason: the four absorption hypotheses do
  not follow from readback alone. Readback constrains `reflect`
  only at the reflected edge's own axiom environment, so no
  instance has the right head. The residue reduced to each hand's
  unit law at its own twist, which the tiers then supplied.
- Failed strategy, with reason: two profile carriers died on a
  cut. Readback pins constant readers to their own value while
  mixed composites manufacture readers constant at a projection,
  so finite reader carriers cannot decide the (D′) profile.
- Process finding: an unstaged research prompt sent a session into
  three turns of token-limit rumination with no output. The
  repairs that worked: exact signatures in the prompt, a mandated
  checker contact per thinking block, hole-and-move-on caps, and
  two-attempt bounds on searches.
- Process finding: a truncated search reported as a negative
  produced a false BLOCKED (`image-fibers-contr→is-embedding`
  exists at `Core/Function/Embedding.lagda.md:278`). Negative
  existence claims now require unbounded searches.

## Verification state

- verified: everything in `Cat.Logic.Gist.ThunkableSquare`,
  `Gist.ReadbackTorsor`, `Gist.BalancedBase`,
  `Gist.BalancedProfile`, and the record cut, by recorded
  `just check` and `just check-tree` runs. Standing counts after
  the cut: `src/Cat` 69 of 69, `src/Test` 33 of 33 at cut time
  and 30 after the one-twist archive move, `src/Bb` 16 of 16.
  Obligation inventory zero throughout: no holes, no postulates.
- verified: `resources-verify` clean, 16 entries, 17 hashes, after
  the rename and the new entry.
- inferred: interchange does not follow at (D′), an absence and
  not a refutation. The (D′)-hcategory convergence and the NbE
  reading are readings, not theorems. The torsor lemma is
  parametrized per filler, and the claim that every doctrinal
  filler is centre-equivariant is an expectation.
- unverified: the sVec two-ribbon-structures anchor and the
  Führmann thunkable-equals-value line, neither on the shelf. The
  two duploid sources remain unaudited, and
  `kiselyov-having-effect` is PROVISIONAL with no statement audit.
- blocked: none.

## Open questions and risks

1. The (D′) associates profile, open in both directions. The
   oracle is the free balanced word model.
2. The two duploid source audits gate every ledger citation that
   leans on them.
3. The archive imports the live `Core.Rx`, accepted risk.
4. The C-F paper (TACS 1994) is a candidate entry, cited at l.514
   of the vendored Kiselyov page.

## Next steps

The word model of the framed point at (D′) strength, line 9 item
1, which now doubles as the profile oracle. The record cut changed
its grammar: both unit laws per hand are theorems, so pure twist
words reduce hard and the surviving grade should be the double
twist. Conjecture to measure: the endo-homs of the free balanced
point are ℤ-graded. After it: morphisms (line 9 item 2), then the
seam's indiscernibility statement, and the `Bb.VgCategoryShape`
successor program.

## Artifacts

- Library: `src/Cat/Logic/Gist/{ThunkableSquare,ReadbackTorsor,
  BalancedBase,BalancedProfile}.lagda.md`, the `Gist` namespace,
  and the record cut's tree (logged separately in
  `notes/2026-07-28-balanced-record-cut.md`).
- Records: `src/Cat/Logic/TODO.md` (the settled blocks, the three
  programs, the balance dossier, the seam), `docs/gloss.md`
  (T32-T34, T31 fix), `docs/guidelines/elaboration.md` (eta
  paragraph, from the cut session).
- Resources: `resources/mmmm-classical-notions/` (renamed),
  `resources/kiselyov-having-effect/` (new, PROVISIONAL).
- Commits this session: `7f1cf05` (Gist vendoring and the
  thunkability verdict), `5ca957e` (next-session programs and the
  mode ruling), `920a21f` (the balance dossier), `1003899`
  (resources custody), `b43fd3c` (ledger and attribution). Sibling
  sessions: `0065395` (the three balance spikes), the record cut,
  and `7fae4ef` (`Bb.VgCategoryShape`).

## Source anchors

- CatColab RFC 0004: <https://next.catcolab.org/rfc/0004>,
  SOURCE-CHECKED at fetch depth.
- Kiselyov, Having an Effect:
  <https://okmij.org/ftp/Computation/having-effect.html>, vendored,
  thesis at l.454, `ReqVar` at l.775 of the vendored copy.
- Sterling's duploids: `~/TypeTopology/source/Duploids`, local
  checkout, the origin mechanization, structure theorem absent.
